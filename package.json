{
  "name": "vhs-vault",
  "version": "1.0.0",
  "private": true,
  "description": "VHS Vault — a mobile-first VHS collection inventory tracker built for Netlify",
  "type": "module",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@netlify/blobs": "^10.7.12"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0"
  }
}
